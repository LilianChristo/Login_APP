import { ClienteRouting.Module } from './cliente-routing.module';

describe('ClienteRouting.Module', () => {
  it('should create an instance', () => {
    expect(new ClienteRouting.Module()).toBeTruthy();
  });
});
